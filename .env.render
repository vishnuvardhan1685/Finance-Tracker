# Render Environment Variables Template
# Copy these variable names to your Render dashboard
# DO NOT commit actual values - this is just a template!

# ============================================
# Backend API Service Environment Variables
# ============================================

# Node Environment (always production on Render)
NODE_ENV=production

# Server Port (Render automatically assigns this)
PORT=5001

# MongoDB Atlas Connection String
# Get this from your MongoDB Atlas dashboard
# Format: mongodb+srv://<username>:<password>@cluster0.xxxxx.mongodb.net/finance-tracker?retryWrites=true&w=majority
MONGO_URI=your_mongodb_atlas_connection_string

# JWT Secret (for authentication tokens)
# Use a strong random string (at least 32 characters)
# You can generate one with: openssl rand -base64 32
# Or let Render auto-generate it
JWT_SECRET=your_strong_random_jwt_secret_here

# Frontend URL (CORS)
# This will be your frontend service URL on Render
# Format: https://your-frontend-name.onrender.com
FRONTEND_URL=https://finance-tracker-frontend.onrender.com


# ============================================
# Frontend Static Site Environment Variables
# ============================================

# Backend API URL
# This will be your backend service URL on Render
# Format: https://your-api-name.onrender.com
VITE_API_URL=https://finance-tracker-api.onrender.com


# ============================================
# Notes for Render Setup
# ============================================

# 1. For Backend Service:
#    - Set all variables in the "Backend API Service" section above
#    - JWT_SECRET can be auto-generated by Render
#    - Update FRONTEND_URL after frontend is deployed

# 2. For Frontend Static Site:
#    - Only set VITE_API_URL
#    - Update this after backend is deployed

# 3. IMPORTANT:
#    - Never commit .env files with actual values
#    - Use Render's environment variable UI
#    - Update FRONTEND_URL and VITE_API_URL with actual service URLs
